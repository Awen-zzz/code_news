import Vue from 'vue'
import VueRouter from 'vue-router'
// 脚手架生成后就有这文件,vue-router 是vue.js 官方给出的路由解决方案。它只能结合 vue 项目进行使用,能够轻松的管理 SPA (单页面应用)项目中组件的切换。
Vue.use(VueRouter)

const routes = [
]

const router = new VueRouter({
  routes
})

export default router
